# Request: empty (uses latest camera images)
---
# Response
string door_state      # "open" | "closed" | "ajar" | "unknown"
bool is_passable       # whether robot can traverse
string conversation     # human-robot conversation snippet
string error_message   # empty if success, error description otherwise
bool success          # true if estimation succeeded, false if error occurred
